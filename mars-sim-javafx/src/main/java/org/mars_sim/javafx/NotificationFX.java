package org.mars_sim.javafx;
/*
import eu.hansolo.enzo.notification.Notification;
import javafx.scene.image.Image;

public class NotificationFX extends Notification {

	//final Image IMAGE;
	//public static final Image QUOTE_ICON = new Image(MainScene.class.getResourceAsStream("/icons/quote.png"));
	//public static final 
	final static Image QUOTE_ICON = new Image(MainScene.class.getResource("/icons/quote_24.png").toExternalForm());

	//public NotificationFX() {}
	
    public NotificationFX(final String TITLE, final String MESSAGE, final Image IMAGE) {
    	super(TITLE, MESSAGE, QUOTE_ICON);
    	//this.TITLE   = TITLE;
        //this.MESSAGE = MESSAGE;
        //this.IMAGE   = IMAGE;
    }

    **
     * Show a Notification with the given parameters on the screen
     * @param TITLE
     * @param MESSAGE
     * @param IMAGE
    *
    @Override
	public void notify(final String TITLE, final String MESSAGE, final Image IMAGE) {
        super.Notifier.INSTANCE.notify(TITLE, MESSAGE, IMAGE);
    }
	 
}
*/