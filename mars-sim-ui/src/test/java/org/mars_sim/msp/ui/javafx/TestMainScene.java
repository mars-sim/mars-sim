
package org.mars_sim.msp.ui.javafx;
/*
import org.junit.Test;
import org.testfx.framework.junit.ApplicationTest;

import javafx.scene.Scene;
import javafx.stage.Stage;

// ApplicationTest is from testFX
// https://github.com/TestFX/TestFX#features
// http://www.bennet-schulz.com/2015/05/how-to-test-your-javafx-fxml-with-testfx.html
// Note: org.loadui's testFx is 3.1.2; starting version, it's renamed to org.testfx
public class TestMainScene extends ApplicationTest {
    @Override
    public void start(Stage stage) {
    	new MainScene(stage);
        Scene scene = new Scene(null);
        stage.setScene(scene);
        stage.show();
    }



    @Test
    public void should_drag_file_into_trashcan() {
        // given:
        rightClickOn("#desktop").moveTo("New").clickOn("Text Document");
        write("myTextfile.txt").push(ENTER);

        // when:
        drag(".file").dropTo("#trash-can");

        // then:
        verifyThat("#desktop", hasChildren(0, ".file"));
    }

}
 */
